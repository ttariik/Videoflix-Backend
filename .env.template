# Django Settings
DEBUG=True
SECRET_KEY=django-insecure-videoflix-backend-secret-key-2024
ALLOWED_HOSTS=tarik-sabanovic.developerakademie.net,localhost,127.0.0.1

# Database Settings
DB_NAME=videoflix
DB_USER=tariksabanovic
DB_PASSWORD=your_postgres_password
DB_HOST=localhost
DB_PORT=5432

# Redis Settings
REDIS=redis://localhost:6379/0
LOCAL_HOST=localhost
RQ_PASSWORD=foobared
REDIS_PORT=6379
REDIS_DB=0
REDIS_USERNAME=default
REDIS_USE_SSL=False
REDIS_SOCKET_TIMEOUT=5
REDIS_SOCKET_CONNECT_TIMEOUT=5
REDIS_RETRY_ON_TIMEOUT=True
REDIS_MAX_CONNECTIONS=10

# Email Settings - Choose one provider configuration and comment out others

# Gmail Configuration
EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USE_TLS=True
EMAIL_USE_SSL=False

# Outlook/Hotmail Configuration
# EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
# EMAIL_HOST=smtp-mail.outlook.com
# EMAIL_PORT=587
# EMAIL_USE_TLS=True
# EMAIL_USE_SSL=False

# Yahoo Configuration
# EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
# EMAIL_HOST=smtp.mail.yahoo.com
# EMAIL_PORT=587
# EMAIL_USE_TLS=True
# EMAIL_USE_SSL=False

# ProtonMail Configuration
# EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
# EMAIL_HOST=smtp.protonmail.ch
# EMAIL_PORT=587
# EMAIL_USE_TLS=True
# EMAIL_USE_SSL=False

# Web.de Configuration
# EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
# EMAIL_HOST=smtp.web.de
# EMAIL_PORT=587
# EMAIL_USE_TLS=True
# EMAIL_USE_SSL=False

# GMX Configuration
# EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
# EMAIL_HOST=mail.gmx.net
# EMAIL_PORT=587
# EMAIL_USE_TLS=True
# EMAIL_USE_SSL=False

# Common Email Settings (Update these with your email credentials)
EMAIL_HOST_USER=your-email@example.com
EMAIL_HOST_PASSWORD=your-password
DEFAULT_FROM_EMAIL=your-email@example.com

# CORS Settings
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000,http://localhost:4200,https://tarik-sabanovic.developerakademie.net
CSRF_TRUSTED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000,http://localhost:4200,https://tarik-sabanovic.developerakademie.net

# Media Settings
MEDIA_ROOT=media/
MEDIA_URL=/media/

# Cache Settings
CACHE_TTL=900  # 15 minutes in seconds

# Security Settings
SECURE_SSL_REDIRECT=True
SESSION_COOKIE_SECURE=True
CSRF_COOKIE_SECURE=True

# Debug Toolbar Settings
INTERNAL_IPS=127.0.0.1

# RQ Worker Settings
RQ_DEFAULT_TIMEOUT=360
RQ_DEFAULT_RESULT_TTL=500

# File Upload Settings
FILE_UPLOAD_MAX_MEMORY_SIZE=10485760  # 10MB
DATA_UPLOAD_MAX_MEMORY_SIZE=10485760  # 10MB

# Video Processing Settings
VIDEO_MAX_SIZE=104857600  # 100MB
VIDEO_ALLOWED_EXTENSIONS=mp4,avi,mov,wmv,flv,mkv

# Thumbnail Settings
THUMBNAIL_SIZE=(200,150)

# API Settings
API_PAGE_SIZE=10
API_MAX_PAGE_SIZE=100
